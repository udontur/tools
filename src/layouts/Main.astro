---
import ToolCard from "@/components/ToolCard.astro";
import tools from "@/data/tools.json";
---
<div class="flex flex-wrap gap-6">
  {tools.map((tool)=>{
    return (
    <ToolCard
      imageName="hockey.jpg"
      name={tool.name}
      description={tool.description}
      whenMade={tool.whenMade}
      url={tool.url}
    />
    )
  })}
</div>
